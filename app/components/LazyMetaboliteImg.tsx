/**
 * Legacy name for metabolite cards. Prefer {@link XpictLazyMetaboliteImg}.
 * Does not call `/depict` or the prediction API — paints client-side via xpict.
 */
export { default } from "~/components/XpictLazyMetaboliteImg";
