/**
 * Index under /:model/:query — panel is rendered by the parent layout when
 * there is no nested hop child. This route exists so Remix nesting stays valid.
 */
export default function MoleculeFocusIndex() {
  return null;
}
